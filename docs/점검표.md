## 1) 기능·엔드포인트

* [x] 핵심 리소스 4개 이상(예: users/posts/comments/categories 등)
* [x] 각 리소스 CRUD 전부 구현(예외: auth는 인증 전용)
* [x] HTTP 엔드포인트 총 30개 이상(메서드+URL 기준)
* [x] 관계형 sub-resource 최소 몇 개 포함(예: `/posts/{id}/comments`)
* [x] 고급 기능 엔드포인트 포함(좋아요/비활성화/통계/파일업로드 등)

## 2) 인증·인가(JWT/RBAC)

* [x] `POST /auth/login` (Access Token 발급)
* [x] `POST /auth/refresh` (재발급)
* [x] `POST /auth/logout`(구현 시 동작/정책 명확)
* [x] 만료 토큰/위조 토큰 처리(401/`TOKEN_EXPIRED` 등)
* [x] Role 최소 2개(`ROLE_USER`, `ROLE_ADMIN`)
* [x] 관리자 전용 API 3개 이상(예: 유저 정지/통계/전체 조회 등)
* [x] 권한 없는 접근 시 403 일관 처리

## 3) 목록 조회 공통 규격(검색/정렬/페이지네이션)

* [x] 목록 API에 `page`, `size` 적용(기본값/최대값 고정)
* [x] `sort=field,ASC|DESC` 지원
* [x] 검색/필터 조건 2개 이상(예: keyword + category / dateFrom+dateTo)
* [x] 목록 응답에 `content/page/size/totalElements/totalPages/sort` 포함

## 4) 입력 검증 & 에러 규격

* [x] DTO/Schema로 필수값/타입/범위 검증
* [x] 전역 예외 처리로 에러 응답 포맷 통일
* [x] 에러 응답 필드 포함: `timestamp, path, status, code, message, details`
* [x] 대표 에러코드 10종 이상 정의(400/401/403/404/409/422/429/500 등)
* [x] Validation 실패 시 `details`에 필드별 사유 포함
* [x] 404/409/422 케이스가 “실제로” 발생하도록 구현되어 있음

## 5) DB(MySQL) / 마이그레이션 / 시드 / N+1

* [x] MySQL 연동(JCloud 포함)
* [x] FK 제약조건 적용
* [x] 검색/조인 대상 인덱스 존재
* [x] 마이그레이션 도구 적용(Flyway/Liquibase 등) 또는 동일 수준의 스크립트
* [x] 시드 데이터 총 200건 이상(테이블 분산)
* [x] N+1 방지 전략 명시(fetch join/EntityGraph/batch size 등)
* [x] 페이지네이션 + 컬렉션 fetch join 같은 위험 패턴 회피(필요 시 배치로딩/2-step)

## 6) 보안·성능

* [x] 민감정보는 전부 환경변수(.env)로 분리
* [x] `.env.example` 제공(키 이름/설명 포함)
* [x] Public repo에 `.env`, 키파일(.pem/.ppk), 비밀번호 파일이 절대 없음
* [x] 비밀번호 해시(bcrypt/argon2)
* [x] CORS 설정(허용 도메인 명시)
* [x] 레이트리밋 또는 요청 크기 제한 중 최소 1개 적용
* [x] 요청/응답 요약 로그(메서드/경로/상태/지연시간)
* [x] 에러 스택트레이스 서버 로그 남김(민감정보 제외)

## 7) 헬스체크

* [x] `GET /health` 인증 없이 200
* [x] 응답에 버전/빌드시간 등 포함
* [x] 헬스체크 스크린샷 제출 준비

## 8) Swagger/OpenAPI 문서

* [x] Swagger UI 접근 가능(`/swagger-ui` 등)
* [x] 모든 엔드포인트 요청/응답 스키마 자동화
* [x] 예시(example) 포함(가능하면 성공 응답도)
* [x] 각 엔드포인트에 400/401/403/404/422/500 예시 응답 문서화
* [x] JWT 보안 스키마 설정(Authorize로 테스트 가능)

## 9) Postman 컬렉션

* [x] 컬렉션(JSON) 파일 포함
* [x] 환경변수 사용(서버 URL, 토큰)
* [x] Pre-request/Test 스크립트 5개 이상
    * [x] 로그인 후 토큰 저장
    * [x] 요청 헤더에 토큰 자동 주입
    * [x] 상태코드/필드 검증(assert)
    * [x] 에러 케이스 3개 이상 검증 포함

## 10) 테스트(자동화 20개+)

* [x] 테스트 총 20개 이상(단위/통합 혼합 가능)
* [x] 인증 필요한 엔드포인트 테스트 포함
* [x] 성공/실패 케이스 모두 포함(401/403/404/409/422 등)
* [x] CI(선택) 붙였다면, 통과 여부 확인

## 11) 배포(JCloud)

* [x] JCloud에서 실제 접속 가능(Base URL 확인)
* [x] 포트 리다이렉션 규칙 반영
* [x] 서버 재시작 후에도 지속 구동(PM2/systemd/Supervisor 중 1개)
* [x] 배포 환경에서도 DB 연결/마이그레이션/시드 재현 가능
* [x] Swagger/Health가 배포 서버에서 정상 접근

## 12) GitHub 레포 구조/제출물

* [x] Public GitHub Repo에 소스 전체 포함
* [x] `README.md` 필수 섹션 전부 포함
    * [x] 프로젝트 개요/기능
    * [x] 실행 방법(로컬)
    * [x] 환경변수 설명(.env.example 매칭)
    * [x] 배포 주소(Base/Swagger/Health)
    * [x] 인증 플로우
    * [x] 역할/권한표
    * [x] 예제 계정
    * [x] DB 연결 정보(테스트용)
    * [x] 엔드포인트 요약표
    * [x] 성능/보안 고려사항
    * [x] 한계/개선
* [x] `docs/` (api-design/db-schema/architecture) 채움
* [x] `postman/` 컬렉션 파일 포함
* [x] `.gitignore`에 `.env`, 키파일, credential 파일 포함되어 있음

## 13) 별도 제출 파일(클래스룸)

* [ ] DB 아이디/비밀번호/접속 명령어 포함 텍스트/워드 파일
* [ ] 그 외 사용 중인 ID/비밀번호 파일(요구되는 것만)
* [ ] JCloud 접속 키 파일(.pem/.ppk)
* [ ] `.env` 파일(절대 GitHub에 업로드 X)